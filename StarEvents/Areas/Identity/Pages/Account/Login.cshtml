@page
@model LoginModel

@{
    ViewData["Title"] = "Log in";
}

<style>
    body {
        background-color: #2b2b2b;
        color: #faf5ef;
    }

    .card {
        background-color: #1f1f1f;
        border-radius: 12px;
        color: #faf5ef;
    }

    .form-control, .form-check-input {
        background-color: #2b2b2b;
        color: #faf5ef;
        border: 1px solid #444;
    }

        .form-control:focus {
            background-color: #2b2b2b;
            color: #faf5ef;
            border-color: #faf5ef;
            box-shadow: none;
        }

    .btn-theme {
        background-color: #faf5ef;
        color: #2b2b2b;
        font-weight: bold;
    }

        .btn-theme:hover {
            background-color: #e0dbd4;
            color: #000;
        }

    .logo {
        display: block;
        margin: 0 auto 20px auto;
        max-width: 150px;
    }
</style>

<div class="d-flex justify-content-center align-items-center" style="min-height: 100vh;">
    <div class="col-md-4">
        <section class="card shadow p-4 text-center">

            <!-- Logo -->
            <img src="~/images/StarEvents.png" alt="Star Events Logo" class="logo" />

            <!-- Welcome Message -->
            <h2 class="mb-3">Welcome to Star Events</h2>
            <p class="text-muted">Please log in to continue</p>

            <form id="account" method="post" class="text-start">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>

                <div class="form-floating mb-3">
                    <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                    <label asp-for="Input.Email">Email</label>
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>

                <div class="form-floating mb-3">
                    <input asp-for="Input.Password" type="password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="password" />
                    <label asp-for="Input.Password">Password</label>
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>

                <div class="checkbox mb-3">
                    <label asp-for="Input.RememberMe" class="form-label">
                        <input class="form-check-input" asp-for="Input.RememberMe" />
                        @Html.DisplayNameFor(m => m.Input.RememberMe)
                    </label>
                </div>

                <div class="d-grid">
                    <button id="login-submit" type="submit" class="btn btn-theme btn-lg">Log in</button>
                </div>

                <div class="text-center mt-3">
                    <p>
                        <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl" style="color:#faf5ef;">Register as a new user</a>
                    </p>
                </div>
            </form>
        </section>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
